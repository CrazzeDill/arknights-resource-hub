// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://crazzedill.github.io',
    base: '/arknights-resource-hub',
    trailingSlash: 'never'
});
